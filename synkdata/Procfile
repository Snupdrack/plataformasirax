web: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers ${WEB_CONCURRENCY:-2}
release: alembic upgrade head && python scripts/create_admin.py --email ${SYNKDATA_ADMIN_EMAIL:-admin@synkdata.mx} --password ${SYNKDATA_ADMIN_PASSWORD}
